# 复制此文件为 .env 并填入你的 API Key
# 商汤日日新 SenseNova API 配置
#
# 安全说明：
# - Web 端：密钥由 server.js 持有（环境变量 AI_API_KEY），
#   前端请求 /ai-proxy/* 时不携带密钥，VITE_AI_API_KEY 仅 APK 直连用。
# - APK 端：CapacitorHttp 直连商汤 API 必须本地持有密钥，读取此变量。
#
# VITE_AI_API_KEY：仅用于 Vite dev server 代理注入 + APK 直连
VITE_AI_API_KEY=your-api-key-here
VITE_AI_BASE_URL=/ai-proxy/v1
VITE_AI_MODEL=sensenova-6.7-flash-lite
#
# AI_API_KEY：仅 server.js（demo 服务器）使用，启动 demo 时通过环境变量传入
#   启动方式：AI_API_KEY=sk-xxx node server.js
#   注意：此变量不会进入 Vite 客户端构建产物
