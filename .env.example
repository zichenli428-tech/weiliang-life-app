# 复制此文件为 .env 并填入你的 API Key
# OpenCode Zen API 配置（模型 mimo-v2.5-free，支持文字+图片+视频识别）
#
# 安全说明：
# - Web 端：密钥由 server.js 持有（环境变量 AI_API_KEY），
#   前端请求 /ai-proxy/* 时不携带密钥，VITE_AI_API_KEY 仅 APK 直连用。
# - APK 端：CapacitorHttp 直连 OpenCode Zen API 必须本地持有密钥，读取此变量。
#
# VITE_AI_API_KEY：仅用于 Vite dev server 代理注入 + APK 直连
VITE_AI_API_KEY=your-api-key-here
VITE_AI_BASE_URL=/ai-proxy/v1
VITE_AI_MODEL=mimo-v2.5-free
#
# AI_API_KEY：仅 server.js（demo 服务器）使用，启动 demo 时通过环境变量传入
#   启动方式：AI_API_KEY=sk-xxx node server.js
#   注意：此变量不会进入 Vite 客户端构建产物
#
# 和风天气 API 配置（免费开发版，用于首页实时天气显示）
# 1. 注册 https://dev.qweather.com/ 账号
# 2. 在控制台 https://console.qweather.com/setting 获取你的专属 API Host
# 3. 添加凭据获取 API Key
# 4. 填入下方两个变量
# 和风天气 2026 年起停用公共域名，必须使用专属 Host + X-QW-Api-Key 请求头认证
# 不配置时首页不显示天气胶囊（优雅降级）
VITE_QWEATHER_KEY=
VITE_QWEATHER_HOST=
