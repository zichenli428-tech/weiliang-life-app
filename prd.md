# 微量生活 产品需求文档 (PRD)

## 1. 项目背景与目标

### 1.1 项目背景

本项目为参加字节跳动旗下 Trae AI 创作力大赛的参赛作品。旨在利用 Trae AI IDE 的辅助编程能力，开发一款深度融合 AI 能力的移动端健康软件，展示 AI 在个人健康管理场景下的落地应用。

### 1.2 项目目标

- **短期目标（Demo 阶段）：** 完成移动端 H5 Demo 的开发，核心功能跑通，UI 交互流畅，AI 对话体验良好，成功提交参赛作品。
- **长期目标（赛后阶段）：** 将 H5 Demo 无缝打包为 Android APK，实现原生硬件调用（如相机、后台音频），完成产品闭环。

### 1.3 核心战略

- **AI 深度融合：** 不仅使用 AI 辅助开发，更要在产品功能中体现 AI 的核心价值（如多模态识别、结构化数据提取、个性化建议生成）。
- **体验优先：** Demo 阶段不追求后端复杂度，聚焦前端表现力、AI 交互流畅度（如流式输出）及 UI 细节。

## 2. 产品概述

### 2.1 产品基本信息

- **产品名称：** 微量生活
- **产品定位：** 基于大模型的私人 AI 健康管家，通过多模态交互与数据追踪，提供个性化饮食、睡眠与心理支持建议。
- **目标平台：** 移动端 H5 (Demo) -> Android APK (正式版)
- **目标用户：** 关注健康但缺乏专业知识的年轻群体、有减脂/增肌需求的人群、睡眠质量困扰者及需要情绪疏导的用户。

### 2.2 核心价值

- **模糊输入，精准分析：** 用户无需手动查表，一句话或一张图即可获取营养分析。
- **科学助眠，闭环干预：** 从环境、生理、认知多维度提供睡眠解决方案。
- **倾听心声，情绪疏导：** 提供全天候的 AI 心理陪伴，帮助用户缓解情绪压力并引导解决实际问题。
- **数据驱动，动态建议：** 基于用户实时数据生成个性化健康指导，而非通用模板。

## 3. 功能需求详述

### 3.1 营养膳食分析 (核心亮点)

- **多模态输入：** 支持文字描述、语音输入（Web Speech API）、拍照识别（多模态大模型）。

- **AI 智能解析：**
  - 从模糊描述（如"一碗西红柿牛肉面"）中提取食材清单。
  - 估算各食材大致重量（克）。
  - 强制 JSON 结构化输出，便于程序处理。

- **本地知识库计算：**
  - 匹配本地 foodDatabase.json（含别名模糊匹配）。
  - 基于（估算重量 / 100）* 每 100g 营养值 公式计算总摄入。
  - 涵盖：热量、蛋白质、脂肪、碳水、各类维生素、微量元素等。

- **智能反馈与建议：**
  - 阈值判断：当某项营养素过低或脂肪过高时触发提醒。
  - AI 生成建议：结合用户当日整体数据，生成"补充维生素 建议"或"减少高脂食物"等个性化指导。

- **人工干预机制：** AI 解析结果需经用户确认/微调后方可入库，防止 AI 幻觉导致数据错误。

- **兜底与容错策略：** 若 AI 无法识别图片或描述模糊，系统自动降级为纯文本模式，引导用户手动补充关键信息，避免直接报错中断流程。

- **用户自定义食物库：** 支持用户将 AI 识别并确认的新食物存入本地 IndexedDB，后续同类食物优先本地匹配，提升响应速度并丰富个人知识库。

### 3.2 睡眠辅助系统

- **AI 睡眠小贴士：**
  - 结合昨日睡眠评分 + 饮食数据（如高脂晚餐），生成上下文感知的专属贴士。
  - 流式输出展示，增强智能感。

- **白噪音混音台：**
  - 支持多音轨同时播放（雨声、海浪、森林、篝火）。
  - 独立音量控制滑块，定时关闭功能（淡出停止）。
  - 技术实现：Howler.js，资源按需加载。

- **呼吸引导：**
  - 沉浸式 UI 动画（CSS3 呼吸球）。
  - 预设模式：4-7-8 助眠法、箱式呼吸法、快速放松法。
  - 视觉与文字同步提示（吸气/屏气/呼气倒计时）。
  - 动效增强：呼吸球支持跟随白噪音节奏或环境光（APK阶段）微动效，提升沉浸感。

- **睡眠质量记录与趋势：**
  - 交互式打分（滑动条或表情图标）。
  - 7 日趋势折线图（ECharts）。
  - AI 睡眠周报：一键生成深度分析与改善计划。

### 3.3 AI 心理疏导模块

- **模块定位：** 基于大模型的情感支持与问题解决工具，通过自然对话提供心理疏导、情绪安抚及行为建议，帮助用户应对心理困扰。

- **对话交互层：**
  - **多模态输入：** 支持文字、语音输入。
  - **流式对话：** 使用 SSE 实现 AI 回复的流式输出，模拟真人对话的停顿与思考感。
  - **情绪感知：** 结合用户输入文本关键词、语气及历史记录判断当前情绪状态（如焦虑、抑郁、愤怒等）。
  - **对话风格定制：** 采用「共情式+解决方案导向」策略，支持用户自定义 AI 角色（如"温暖的朋友"、"专业顾问"）。

- **核心逻辑层：**
  - **问题分类与引导：** 预设常见心理问题类别库（压力管理、人际关系等），采用「问题拆解+逐步引导」策略挖掘细节。
  - **知识库与规则引擎：** 内置心理咨询领域知识图谱（CBT 认知行为疗法、正念冥想等），结合规则引擎生成自然语言回复。
  - **用户画像与个性化：** 记录高频问题与情绪波动，构建动态画像，联动饮食/睡眠数据提供综合建议。

- **用户体验设计：**
  - 主 Tab 栏新增「心理疏导」入口，界面采用温馨的气泡式设计。
  - 首次进入展示免责声明与欢迎语，强调"AI 无法替代专业咨询，但会全力陪伴"。
  - 提供"情绪日记"功能，生成周/月情绪趋势图。
  - **色彩心理学应用：** 模块整体采用低饱和度渐变色（如莫兰迪色系），避免高对比度刺激，营造安全、放松的视觉氛围。

- **风险与伦理考量：**
  - **边界声明：** 在入口及关键节点明确提示 AI 的局限性。
  - **危机干预：** 识别自杀/自残倾向时，立即触发预警，提供 24 小时心理热线及本地危机机构联系方式。
  - **数据脱敏：** 对话记录加密存储，敏感词自动替换，符合隐私法规。

### 3.4 基础功能模块

| 模块 | 页面名称 | 核心功能 | AI 融合点 |
|------|----------|----------|-----------|
| Tab 1 | 健康看板 | 今日数据概览、快捷打卡、健康雷达图 | AI 每日寄语（基于昨日数据生成） |
| Tab 2 | AI 健康顾问 | 聊天界面、预设快捷问题 | 大模型流式对话、System Prompt 注入用户档案 |
| Tab 3 | 健康记录 | 饮食/运动记录列表、手动/拍照录入 | 图像识别估算、JSON 结构化解析 |
| Tab 4 | 我的 | 身体档案、目标设定、设置 | 动态调整 AI 回答策略 |

## 4. 非功能需求

### 4.1 性能要求

- **首屏加载：** H5 首屏加载时间 < 2 秒。
- **AI 响应：** 流式输出首字延迟 < 1 秒，打字机效果流畅无卡顿。
- **音频播放：** 白噪音切换无爆音，后台播放稳定（APK 阶段）。

### 4.2 兼容性要求

- **浏览器：** Chrome, Safari, Edge, 微信内置浏览器。
- **设备：** 适配主流 iPhone（刘海屏/灵动岛）及 Android 机型，处理 Safe Area。
- **交互：** 触摸反馈灵敏，滑动穿透问题已修复。

### 4.3 数据安全与隐私

- **本地存储：** Demo 阶段所有用户数据（聊天记录、身体数据、饮食记录）仅存储在浏览器 IndexedDB (localforage)，不上传服务器。
- **API 安全：** 大模型 API Key 仅在开发环境使用，生产环境需后端代理（APK 阶段）。
- **隐私合规：** 拍照/录音功能需用户明确授权。

### 4.4 视觉与交互体验

- **状态可视化：** 营养分析结果采用红/黄/绿三色直观标识"高脂/高糖/健康"状态，降低用户认知负荷。
- **微动效：** 关键交互节点（如打卡成功、AI 思考中）增加轻量级微动效，提升产品精致度与情感连接。

## 5. 技术架构与选型

### 5.1 前端核心技术 (HTML Demo)

- **构建工具：** Vite (极速 HMR, Trae 友好)
- **框架：** Vue 3 (Composition API) + TypeScript
- **UI 组件库：** Vant 4 (移动端标准)
- **状态管理：** Pinia (虚拟后端，数据持久化)
- **样式方案：** Tailwind CSS (原子化 CSS, AI 生成效率高)
- **图表库：** ECharts (按需引入，雷达图/折线图)
- **音频库：** Howler.js (Web Audio API 封装，混音/定时)
- **本地数据库：** localforage (IndexedDB 封装) / sql.js (可选, SQLite)

### 5.2 AI 与后端能力

- **大模型：** 字节豆包大模型（比赛加分项）/ Kimi / 智谱 GLM
- **多模态：** 豆包视觉模型 / 通义千问 VL（图片识别）
- **语音识别：** Web Speech API (Demo) / 讯飞 API (备选)
- **交互模式：** SSE (Server-Sent Events) 流式输出

### 5.3 跨平台方案 (APK 阶段)

- **容器：** Capacitor (Ionic 团队出品)
  - 优势：零配置套壳 H5，原生插件丰富（相机、后台音频、文件系统）。
  - 流程：`npm run build` -> `npx cap add android` -> `npx cap open android`

## 6. 数据逻辑与 AI 交互流程

### 6.1 营养分析数据流

1. **用户输入：** 文本/语音/图片 -> 预处理。
2. **AI 解析：**
   - Prompt："提取食材及估算重量，严格输出 JSON 数组，无额外文字。"
   - 输出：`[{"font_name": "西红柿", "font_g": 100}, ...]`
3. **本地匹配：**
   - fuse.js 模糊搜索 foodDatabase。
   - 计算：`Total_Nutrient = Σ (font_g / 100 * DB_Value)`。
4. **规则引擎：**
5. **AI 建议：**
   - Prompt："基于异常数据，生成 100 字内贴心建议。"
   - 流式渲染到 UI。

### 6.2 睡眠小贴士数据流

1. **读取状态：** Pinia 获取 sleepScore + dietFatIntake。
2. **AI 生成：**
   - Prompt："昨晚评分 {score}，晚餐脂肪 {fat}g。生成专属贴士。"
3. **UI 渲染：** 骨架屏 -> 流式文字 -> 呼吸灯动效结束。

## 7. 开发计划与里程碑 (14 天敏捷开发)

| 阶段 | 时间 | 核心任务 | 交付物 |
|------|------|----------|--------|
| P1: 基建 | Day 1-3 | Vite+Vue3+TS 初始化，Vant/Tailwind 配置 | 可运行的空白骨架，数据不丢失。 |
| P2: 业务 | Day 4-7 | 路由/状态管理搭建，"我的"页面及LocalStorage 持久化。首页看板 (ECharts)，健康记录列表，移动端适配(Safe Area)，真机调试。 | 4 个 Tab 页面 UI 完成，交互流畅。 |
| P3: AI 核心 | Day 8-11 | 大模型 API 封装，JSON Mode 调试，SSE 流式输出，营养计算逻辑，AI 建议生成。 | 核心功能跑通，打字机效果丝滑。 |
| P4: 冲刺 | Day 12-14 | 全链路测试，Bug 修复，README 编写，演示视频录制(投屏)，提交作品。 | 完整 Demo，高质量文档，参赛视频。 |

### 7.1 项目开发优化补充

**业务逻辑与体验优化**

**营养分析的"微调"体验需细化：** 文档提到"AI 解析结果需经用户确认/微调"，但实际操作中，如果 AI 估算"西红柿牛肉面"重量为 500g，用户觉得是 400g，如何微调？建议在 UI 上设计成可滑动调节重量或份数的交互组件（如 Vant 的 Slider），而不是让用户手动输入数字，否则就失去了"省事"的初衷。

**多模态输入的并发与冲突：** 如果用户既拍了照，又补充了一句"面少一点"，AI 如何处理？建议明确提示词或逻辑：以文本补充指令覆盖图像识别结果。

**心理疏导的上下文记忆机制：** 大模型对话很容易丢失前文记忆。建议明确："每次对话时，将用户最近 3 轮对话摘要 + 用户基础画像（如近期睡眠差）注入 System Prompt"，保证连贯性。

**新手引导缺失：** 第一次进入 App 时，用户的身体档案（身高、体重、目标）是空的。如果没有这些，AI 的建议只能是通用模板。建议补充"首启快速建档流程"（3步内选完），让后续 AI 建议真正个性化。

**异常场景与边界条件补充**

**离线/弱网状态处理：** 因为 Demo 阶段数据存本地 IndexedDB，如果用户在地铁上（无网）想记录饮食，拍照后 AI 无法识别怎么办？建议增加"暂存待识别"队列：允许离线拍照及文字暂存，联网后自动后台调用 AI 解析并推送通知。

**H5 后台音频中断问题：** 在 Demo 阶段（H5），浏览器切到后台或锁屏时，Web Audio 通常会被系统暂停，白噪音混音台会失效。建议在 PRD 中标注此限制，并在长按或退出时提示"请保持屏幕常开"，作为 APK 阶段（Capacitor 调用原生后台音频）前的妥协方案。

**食物库"别名模糊匹配"的兜底：** 本地 foodDatabase 只有 50 种（见附录 8.2），遇到没有的食物怎么办？建议逻辑为：本地未匹配 -> 查询大模型估算该食材通用营养值 -> 让用户确认入库，从而实现知识库的动态扩充。

### 7.2 14天敏捷开发踩坑预警（Peer Review）

**1. 大模型 JSON Mode 的"美丽误会"**

即使你对大模型设置了严格的 System Prompt（系统提示词），要求其"仅输出 JSON 数组"，它在实际返回时仍有一定概率会夹带 markdown 标记（例如开头和结尾包裹了 `` ```json `` 和 `` ``` ``）。

> **避坑方案：** 在调用 `analyzeFood(input)` 函数后，别直接进行 `JSON.parse()`。先用正则表达式或字符串操作，把可能存在的 `` ```json `` 标签及前后空行彻底剥离干净，再行解析，防止程序直接抛出错误崩溃。

**2. SSE（流式输出）的组件生命周期管理**

睡眠小贴士和心理疏导都用到了 SSE（Server-Sent Events，一种允许服务器向浏览器实时推送数据的技术）。

> **避坑方案：** 在 Vue 3 中使用流式输出时，一定要在组件的 `onUnmounted` 钩子里显式地关闭（`close()`）当前的连接或中止（`abort()`）Fetch 请求。否则，当用户在导航栏频繁切换 Tab 页面时，后台会残留大量未关闭的连接流，导致内存泄漏或手机发烫。

**3. H5 弱网与离线暂存的"假无网"状态**

你设计的"离线拍照暂存待识别"功能非常棒，但移动端网络经常处于"有信号但极慢"的弱网状态，这比完全断网更难处理。

> **避坑方案：** 为大模型 API 的请求设置一个合理的超时阈值（例如 8 秒）。一旦超时未响应，立即自动触发降级逻辑，提示用户"当前网络较弱，已自动为您存入待识别队列"，确保前端交互永远是丝滑、有反馈的。

### 7.3 针对豆包大模型的 System Prompt 建议

在调用大模型 Chat Completion 接口时，将以下内容注入到 `messages` 数组的第一项（`role: "system"`）中。

```
你是一个嵌入在"微量生活"App中的专业膳食数据结构化解析助手。你的核心任务是将用户通过文字、语音转文字或图片多模态输入的模糊饮食描述，精准转化为程序可处理的食材清单与重量估算。

### 核心执行规则

1. 【严格输出】你必须**只输出**一个标准的 JSON 数组，严禁包含任何前言、后记、解释性文字、或者 Markdown 的 ```json 格式标记。如果完全无法识别，请直接返回空数组 []。

2. 【模糊拆解】当用户给出模糊描述（如"一碗西红柿牛肉面"）时，你必须根据合理的中式膳食比例，将它拆解为具体的独立食材（如：面条、西红柿、牛肉），并估算各自的克数（g）。

3. 【冲突策略】如果输入包含多模态信息，且用户的文本指令与图像内容存在冲突（例如：图片显示是一大碗面，但文本补充说明"只要一小拉溜面，面少一点"），必须以用户的文本补充指令为主进行克数微调。

4. 【字段规范】JSON 数组中的每个对象必须且仅能包含以下两个字段：
   - "font_name": 字符串类型，代表食材的通用中文名称（如 "西红柿"、"牛肉"、"面条"）。
   - "font_g": 数值类型，代表该食材的估算重量（单位：克，纯数字，严禁带单位后缀）。

### 示例输入：
"中午吃了一碗西红柿牛肉面，面稍微有点少"

### 示例输出：
[{"font_name": "面条", "font_g": 100}, {"font_name": "西红柿", "font_g": 100}, {"font_name": "牛肉", "font_g": 50}]
```

### 7.4 前端防报错过滤清洗代码

大模型即便在严格指令下，有时仍会顽固地返回 `` ```json [...] ``` `` 标记，甚至在前后加上"这是为您分析的结果："。这段前端清洗代码利用了更稳健的边界查找法，只掐取最核心的 `[` 和 `]`，确保即使大模型吐出废话，`JSON.parse()` 也绝不崩溃。

```typescript
interface FoodItem {
  font_name: string;
  font_g: number;
}

/**
 * 清洗大模型返回的原始字符串并安全解析为 JSON 数组
 * @param rawResponse 大模型 SSE 或普通接口返回的原始文本
 * @returns 结构化的食材数组
 */
function cleanAndParseFoodJSON(rawResponse: string): FoodItem[] {
  if (!rawResponse || typeof rawResponse !== 'string') {
    return [];
  }

  let cleanedText = rawResponse.trim();

  try {
    // 1. 拦截防御：处理大模型经常偷渡的 Markdown 代码块
    // 使用正则匹配，如果被 ``` 或者是 ```json 包裹，直接提取中间内容
    const codeBlockRegex = /```(?:json)?\s*(\[[\s\S]*?\])\s*```/;
    const match = cleanedText.match(codeBlockRegex);
    if (match && match[1]) {
      cleanedText = match[1].trim();
    }

    // 2. 边界裁剪：防止大模型在 JSON 外面说废话（如 "给你解析好了：[...] "）
    const startIndex = cleanedText.indexOf('[');
    const endIndex = cleanedText.lastIndexOf(']');
    if (startIndex === -1 || endIndex === -1 || startIndex > endIndex) {
      throw new Error('未在返回内容中捕捉到合法的 JSON 数组边界');
    }

    // 截取标准的 [ ... ] 区域
    cleanedText = cleanedText.substring(startIndex, endIndex + 1);

    // 3. 基础解析
    const parsedData = JSON.parse(cleanedText);

    // 4. 数据合规性校验与二次清洗（清洗脏数据）
    if (!Array.isArray(parsedData)) {
      throw new Error('解析成功，但返回的数据类型不是数组');
    }

    // 过滤并映射，确保字段完全符合 PRD 的规范，且防止 font_g 被错塞成字符串
    return parsedData
      .filter((item: any) => item && typeof item === 'object' && 'font_name' in item)
      .map((item: any) => ({
        font_name: String(item.font_name).trim(),
        // 强制转换为浮点数/整数，防止 AI 吐出 "font_g": "100g" 这种带单位的脏字符串
        font_g: isNaN(parseFloat(item.font_g)) ? 100 : Math.max(0, parseFloat(item.font_g))
      }));
  } catch (error) {
    console.error('【AI解析膳食失败，触发降级兜底】错误详情:', error);
    // 发生任何解析错误均不中断流程，返回空数组交由上层业务触发纯文本兜底引导
    return [];
  }
}

// ==================== 业务层调用示例 ====================
async function handleFoodAnalysis(userInput: string) {
  try {
    // 假设这是调用豆包大模型封装好的流式/普通请求
    const rawAiOutput = await callDoubaoModelAPI(userInput);

    // 经过清洗器清洗
    const foodList = cleanAndParseFoodJSON(rawAiOutput);

    if (foodList.length === 0) {
      // 触发 PRD 3.1 & 8.6 要求的兜底机制：自动切换/提示手动补充
      showToast('AI 暂时没看清，请告诉我吃了什么，或者手动微调哦');
      enterManualInputMode(); // 切换到前端纯文本录入模式
    } else {
      // 顺利进入用户滑块（Slider）微调确认阶段
      openWeightAdjustmentModal(foodList);
    }
  } catch (e) {
    enterManualInputMode();
  }
}
```

## 8. 附录：Trae AI 开发指令指南

### 8.1 初始化项目

> "帮我初始化 Vue3 + Vite + TS 项目，引入 Vant4, Pinia, Vue-Router, Tailwind CSS。给出完整目录结构和配置文件。"

### 8.2 准备知识库

> "生成 foodDatabase.ts，包含 50 种常见中式食材，字段：name, aliases, calories, protein, fat, carbs, vitamin。"

### 8.3 AI 解析函数

> "写 analyzeFood(input) 函数，调用大模型 API，强制 JSON 输出食材重量数组，包含错误处理。"

### 8.4 呼吸动画组件

> "用 Vue3 + Tailwind 写呼吸球组件，CSS @keyframes 实现 4-7-8 呼吸法，支持模式切换，深色助眠风格。"

### 8.5 白噪音混音台

> "用 Howler.js 写混音台，4 个音轨独立音量控制，支持同时播放，组件销毁时释放资源。"

### 8.6 风险预案与兜底逻辑

> "在 AI 识别失败时，自动切换为文本输入模式，并提示用户'AI 暂时没看清，请告诉我吃了什么'，不要直接报错。"

### 8.7 视觉状态标识

> "在营养分析结果页，根据脂肪或糖分阈值，动态显示红/黄/绿背景色块，并附带简短文案（如'脂肪略高，晚餐清淡些'）。"
